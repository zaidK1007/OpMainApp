{
  "version": 2,
  "builds": [
    {
      "src": "machine-maintenance-dashboard/package.json",
      "use": "@vercel/next"
    },
    {
      "src": "machine-maintenance-backend/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "machine-maintenance-backend/index.js"
    },
    {
      "src": "/(.*)",
      "dest": "machine-maintenance-dashboard/$1"
    }
  ],
  "installCommand": "cd machine-maintenance-backend && npm install && cd ../machine-maintenance-dashboard && npm install",
  "buildCommand": "cd machine-maintenance-backend && npx prisma generate && cd ../machine-maintenance-dashboard && npm run build"
}
