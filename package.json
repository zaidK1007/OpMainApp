{
  "name": "machine-maintenance-app",
  "version": "1.0.0",
  "description": "Machine Maintenance Dashboard - Full Stack Application",
  "private": true,
  "scripts": {
    "install:all": "cd machine-maintenance-dashboard && npm install && cd ../machine-maintenance-backend && npm install",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd machine-maintenance-backend && npm run vercel-build",
    "build:frontend": "cd machine-maintenance-dashboard && npm run build",
    "dev": "concurrently \"cd machine-maintenance-backend && npm run dev\" \"cd machine-maintenance-dashboard && npm run dev\"",
    "setup-db": "node scripts/setup-database.js",
    "deploy": "vercel --prod"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "keywords": [
    "machine-maintenance",
    "dashboard",
    "nextjs",
    "express",
    "prisma",
    "postgresql"
  ],
  "author": "",
  "license": "MIT"
}
